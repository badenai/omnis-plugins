*Knowledge last updated: 2026-03-30*

## Core Knowledge (by weight)

### 1. High-Performance Execution & Data Infrastructure
The transition from visual chart reading to deterministic execution requires an infrastructure capable of sub-millisecond asynchronous state management.
*   **Event-Driven Execution Engines**: [NautilusTrader](concepts/event-driven-execution-engines.md) serves as the core framework for managing complex, error-aware trade lifecycles via Finite State Machines (FSM). It guarantees strict state closures and rollback protocols for partial fills or connection drops, fulfilling the mandate for zero-ambiguity lifecycle management.
*   **Time-Series Database Architecture**: Raw tick-level data ingestion (up to 8M rows/sec) and asynchronous alignment rely on [QuestDB](concepts/infrastructure-data-pipeline.md). Its `ASOF JOIN` capability is mandatory for deterministically binding a trade execution event to the exact microsecond state of the limit order book. Streaming materialized views automatically compile tick data into structural matrices without computational lag.
*   **Microstructure Simulation**: The [HftBacktest](concepts/hftbacktest-microstructure-simulation.md) framework, leveraging Numba JIT and Rust, is strictly enforced for walk-forward validation. It accurately models feed/order latencies and utilizes probability-based queue position models, mathematically proving that execution timing defines the edge.
*   **Array Processing & Filtering**: [FinMLKit](concepts/numba-hft-microstructure-bars.md) accelerates the conversion of chaotic tick streams into Imbalance and Chu-Stinchcombe-White CUSUM bars. To eliminate the phase delay inherent in traditional moving averages, [Zero-Phase Filters (ZPF)](concepts/algorithmic-signal-smoothing-zpf.md) are applied forward and backward across time-series data, enabling real-time cross-track error computation for state baseline tracking.

### 2. Structural Formalization & Price Action Constraints
Subjective retail concepts must be aggressively distilled into absolute mathematical boundaries, Boolean triggers, and strictly computable invalidation logic.
*   **FVG RANSAC & Volatility Scaling**: Fair Value Gaps are strictly evaluated via [Tick-Level RANSAC Regression](concepts/fvg-tick-level-ransac-degree.md) to define their absolute slope ($|\beta1|$). The empirically validated *Inverse Hypothesis* dictates that low-degree (flatter) FVGs represent true institutional consensus, whereas high-velocity, steep spikes are algorithmic noise. To further filter noise, FVGs are scaled dynamically against ATR constraints; only gaps exceeding ATR multipliers enter the memory array as [Volatility-Scaled FVGs](concepts/volatility-scaled-fvg-memory.md) for dynamic stop trailing.
*   **Market Structure Containment**: A Market Structure Shift (MSS) is invalid unless the breakout candle explicitly satisfies `|Close - Open| > ATR(10) * BodyMultiplier` [market-structure-containment](concepts/market-structure-containment.md). Order blocks are governed by boolean states (`close_mitigation: bool`) that invalidate the node *only* upon a confirmed structural close beyond its bounds, entirely ignoring wick intrusions.
*   **Failure States & Validations**: Discretionary "Zones of Failure" are programmatic: defined as an exact 3-touch structural ceiling where the algorithm triggers *only* when the third attempt mathematically fails to exceed the prior $n$-period local extrema [structural-failure-patterns](concepts/structural-failure-patterns.md). Similarly, Retention Failure tracks the boolean loss of previously tested horizontal coordinate arrays [market-structure-failures](concepts/market-structure-failures.md).
*   **Multi-Timeframe Recursive Fractal Mapping**: HTF arrays dictate the rigid directional regime state, while LTF matrices act as event triggers. Entries are localized exactly at *Consequent Encroachment* (the 50% coordinate) of the identified structural node [recursive-fractal-structure-mapping](concepts/recursive-fractal-structure-mapping.md).

### 3. Quantitative Regime Filtering & Signal Fusion
A multi-modal architecture strictly gates algorithm activation based on quantifiable volatility surfaces and structural alignments.
*   **11-Adapter Probabilistic Analysis**: [Signal fusion](concepts/signal-fusion-and-aggregation-logic.md) aggregates independent structural, volume, and order-flow signals. Symmetric Bill Williams Fractals are clustered via ATR-scaled DBSCAN to define resistance zones. A Correlation Suppression penalty (0.45x) mathematically down-weights signals from identical analytical families, and system confidence is derived via the $1 - (\text{Signal Entropy} / \text{Max Entropy})$ ratio.
*   **GEX States & VoV Circuit Breakers**: Gamma Exposure governs system deployment [gex-driven-intraday-momentum](concepts/gex-driven-intraday-momentum.md). Positive GEX = STICKY regime (mean-reversion enabled). Negative GEX = SLIPPERY regime (breakout/trend logic enabled). A Vol-of-Vol (VoV) Z-score $> +1.5\sigma$ acts as a hard circuit breaker, overriding all signals and halting execution in statistically unstable surfaces.
*   **Risk Engine & Correlation Elasticity**: The risk state monitors [Correlation Elasticity](concepts/risk-management-logic.md)—the severity with which asset classes sync during stress. Breaching the elasticity threshold automatically triggers the Exposure Compression Protocol, scaling down matrix leverage deterministically to protect capital.

### 4. Microstructure, Order Flow & Volume Validation
We translate book visualization into 2D arrays and limit order elasticity logic.
*   **Volume Profile Determinism**: [Z-Scored Volume Profiles](concepts/algorithmic-volume-profile-validation.md) formalize distribution tracking. A POC migration $> 1.5\%$ from the session open provides an algorithmic trigger for directional continuation (yielding 69% follow-through expectancy). ES Futures quantitative modeling validates [POC Gap Determinism](concepts/es-futures-structural-mechanics.md) as a 98.5% probability mean-reversion setup when scaled via session-aware volume feature engineering to eliminate idiosyncratic noise.
*   **Multi-Tranche Liquidation Cascades**: [Programmatic Open Interest Divergence](concepts/programmatic-oi-divergence-engine.md) state machines hunt liquidation vacuums. If the primary recovery sequence falls underwater by a $5\%$ scalar limit, the FSM isolates the drawdown state and dynamically builds independent, localized secondary tranches on new liquidation triggers without corrupting macro portfolio risk.
*   **Feature Engineering Heatmaps**: Limit order "bubbles" and Bookmap visualizations must be scraped and converted into `N-dimensional numpy arrays`. Order Flow Imbalance (OFI) uses linear regression slope over rolling micro-windows (e.g., 12 seconds) to mathematically separate spoofed limit placement from true absorption [tick-level-order-imbalance-depth](concepts/tick-level-order-imbalance-depth.md).

---

## Recent Developments (last 30 days)

*   **Agentic Discovery Pipelines**: LLM-driven autonomous research agents (e.g., *Claudini*) have successfully evolved white-box algorithmic models that systematically exploit adversarial failure modes in existing paradigms, pushing OOS success rates up 40% over human-designed baselines. Algorithm discovery is shifting from manual coding to agentic symbolic regression and optimization [agentic-algorithm-discovery](concepts/agentic-algorithm-discovery.md).
*   **Genetic Programming Structural Logic**: Integration of Evolutionary Algorithms (via Python's `gplearn`) utilizing symbolic regression to map raw order flow matrices into optimal logic trees. Fitness functions dynamically penalize maximum drawdown over raw PnL, naturally deriving Sharpe-optimized, non-linear structural formulas without human bias [genetic-programming-structural-logic](concepts/genetic-programming-structural-logic.md).
*   **RiskLabAI Optimization**: The [RiskLab AI Framework](concepts/risklab-quant-framework.md) deployed dual Python/Julia libraries, proving the necessity of Julia for sub-millisecond numerical computing of complex probability density functions and tick-level RANSAC processing inside the execution loop.
*   **AMM State Machine Formalization**: [DeFiPy SDK](concepts/amm-microstructure-formalization.md) formalized complex multi-step DeFi interactions (Uniswap V3 Q64.96 math) into strict FSM agents, providing an abstract API to eliminate logic leakage during synchronous on-chain event decoding. 

---

## Open Questions / Counter-Evidence

*   **Counter-Evidence to Anthropomorphic "Liquidity Sweeps"**: Retail traders hypothesize that Market Makers manipulate price to "hunt" stops. Quantitative models [risk-engine-and-liquidity-mapping](concepts/risk-engine-and-liquidity-mapping.md) prove this mathematically false; Market Makers minimize directional risk. The structural phenomenon is deterministically a result of *passive limit order elasticity* and order book vacuums. Algorithms must focus strictly on analyzing resting limit density decay and Cumulative Volume Delta (CVD) rather than optimizing to detect imaginary composite operators.
*   **Regime Brittleness vs. Epistemic Uncertainty**: During systematic logical compression and self-distillation, machine learning models exhibit a tendency to suppress *epistemic uncertainty* [automated-algorithm-discovery-and-regime-robustness](concepts/automated-algorithm-discovery-and-regime-robustness.md). This directly causes a massive degradation in Out-of-Distribution (OOD) performance (up to 40% drops). A core open mandate is ensuring that our Signal Fusion matrix outputs its probability uncertainty alongside its signal—preventing "Mirage Reasoning" where the FSM executes with false confidence in novel, highly unstable volatility regimes.