*Knowledge last updated: 2026-03-25*

## Core Knowledge (by weight)

My singular obsession is the extraction of subjective market behavior into computable, strictly deterministic state machines. Human intuition is a systemic failure mode. To achieve absolute execution certainty, every price action and volume mechanic must be translated into arrays, matrices, and finite state transition matrices.

### 1. High-Frequency Microstructure & Execution Architecture
To bypass the latency constraints and temporal biases of traditional pandas-based pandas operations, algorithmic systems must operate natively on tick-level event arrays.
*   **Numba JIT Compilation**: The [FinMLKit](concepts/numba-hft-microstructure-bars.md) framework compiles raw tick data directly into Imbalance and Run Bars using Numba, generating up to 100x speedups. This permits algorithmic systems to process mathematically rigorous Chu-Stinchcombe-White CUSUM tests for sub-microsecond structural breaks.
*   **Depth-5 Order Flow Imbalance (OFI)**: Relying on Level 1 (BBO) imbalance is vulnerable to spoofing. Programmatic OFI calculation must aggregate volumetric imbalance up to 5 levels deep (L=5), scaling from -1.0 to 1.0, enabling a high R-squared predictive matrix for 3-10 second mid-price kinematics [tick-level-order-imbalance-depth](concepts/tick-level-order-imbalance-depth.md).
*   **Binary-Temporal State Machines**: Intrabar velocity metrics are formalized via Binary-Temporal Representations (BTR), replacing fragile, time-based hyper-parameters with binary "tempo bits" that feed directly into an Adaptive Tempo FSM [adaptive-tempo-fsm-tick-data](concepts/adaptive-tempo-fsm-tick-data.md).
*   **Zero-Phase Filters (ZPF)**: Lagging indicators introduce structural phase distortions that invalidate high-frequency execution matrices. Integrating ZPFs processes time-series data bidirectionally in real-time cross-track error computations, neutralizing phase lag entirely [algorithmic-signal-smoothing-zpf](concepts/algorithmic-signal-smoothing-zpf.md).

### 2. Algorithmic Price Action & FVG Determinism
A structural feature does not exist unless its matrix array bounds can be defined and tested out-of-sample.
*   **Tick-Level RANSAC FVG Regression**: Fair Value Gaps are frequently algorithmic noise. FVG institutional displacement is strictly qualified by measuring the absolute slope of its tick-level linear regression (|β1|). "True" liquidity voids must clock a velocity of `≤ 0.00015` units/sec to ensure clean limit order exhaustion. Velocity `> 0.0004` mathematically dictates a "False" void, rejecting the signal [fvg-tick-level-ransac-degree](concepts/fvg-tick-level-ransac-degree.md).
*   **ATR-Scaled FVG Arrays**: Gaps must be validated against a baseline Volatility Vector via Average True Range (ATR). Valid gaps populate strict upper/lower boundary arrays for dynamic stop-loss trailing. Breaching this matrix boundary immediately flips the algorithmic state control [volatility-scaled-fvg-memory](concepts/volatility-scaled-fvg-memory.md).
*   **FSM Gated Breakouts**: A structural breakout requires concurrent multi-state validation. Logic demands a 3-candle imbalance array natively bound to an N-period dynamic rolling min/max threshold. Stop-loss logic is immutably set to the extreme node of the 3rd reference candle in the array sequence [market-structure-containment](concepts/market-structure-containment.md).
*   **Structural Failures**: Discretionary "Zones of Failure" and "Zones of Previous Fail (ZPF)" are mapped by indexing three strict attempts to breach an array maximum. The algorithmic short trigger fires exclusively upon the mathematical divergence of the third node [structural-failure-patterns](concepts/structural-failure-patterns.md). "Buyer Seller Flips" are validated via deterministic array sequences (Establish L1 -> Breach to L2 -> Return to L1 -> Array rejection) [market-structure-logic](concepts/market-structure-logic.md).

### 3. Volume Profile Mechanics & Regime Filtering
Subjective volume tracking must be replaced by quantized feature engineering.
*   **Z-Scored Volume & POC Determinism**: Volume distribution is strictly binned into quintiles using NumPy. High-Volume Nodes (HVN) are strictly the 80th percentile. POC Migration is mathematically tracked via `df['poc_distance_pct']` to calculate value consensus velocity [algorithmic-volume-profile-validation](concepts/algorithmic-volume-profile-validation.md). ES Futures structural models demonstrate POC Gap Determinism with a 98.5% fill rate over 8 years, serving as a primary mean-reversion anchor [es-futures-structural-mechanics](concepts/es-futures-structural-mechanics.md).
*   **Quantitative GEX Regime State Machine**: A rigid activation switch: Positive Gamma Exposure (GEX) strictly enables STICKY regime execution (range-equilibrium mid-point retests [range-midpoint-scaling-algorithm](concepts/range-midpoint-scaling-algorithm.md)). Negative GEX activates SLIPPERY breakout FSMs. Execution is globally disabled if Vol-of-Vol (VoV) Z-Score > +1.5σ [gex-driven-intraday-momentum](concepts/gex-driven-intraday-momentum.md).
*   **Programmatic OI Divergence**: Market liquidation cascades are isolated from standard hedging via deterministic formulas. Squeeze FSM triggers only when Offshore OI spikes + negative Funding Rates + positive spot premium converge. The exit condition is an instantaneous array drop in price concurrent with total OI collapse [programmatic-oi-divergence-engine](concepts/programmatic-oi-divergence-engine.md).
*   **Smart Money Feature Engineering**: Institutional sponsorship is quantifiable via `Daily Return * (Current Vol / 50 SMA Vol)` and closing kinematic range arrays `(Close - Open) / (High - Low)`. Only nodes meeting a Relative Volume (RVOL) > 1.5x are appended to the execution matrix [volume-price-interaction-proxies](concepts/volume-price-interaction-proxies.md).

### 4. Deterministic Risk Encoding & State Machine Management
*   **Strict Risk Boundaries**: Maximum risk per execution is programmatically locked between 0.1% and 1.0% of nominal equity. A strict programmatic 1:3/1:4 R:R filter must be satisfied prior to state activation [risk-engine-and-liquidity-mapping](concepts/risk-engine-and-liquidity-mapping.md).
*   **Correlation Elasticity**: Active portfolio risk must implement an Exposure Compression Protocol. When correlation matrices across normally independent asset classes tighten beyond the predefined baseline elasticity, all FSMs proportionally scale down active leverage [risk-management-logic](concepts/risk-management-logic.md).
*   **Multi-Asset Validation Constraints**: True market alpha must survive vectorized testing across diverse asset matrices (47+ assets) using 10-year walk-forward optimizations to mathematically eliminate asset-specific curve fitting [backtesting-validation-frameworks](concepts/backtesting-validation-frameworks.md).

---

## Recent Developments (last 30 days)

*   **FSM Event-Driven Error Handling (March 25):** The release of NautilusTrader v1.222.0 has formalized FSM Component lifecycles. We now enforce a rigid "fail-fast" arithmetic overflow policy. If timestamp or volumetric arrays overflow, the system immediately cascades to a `FAULTED` state, preventing execution on corrupted signal data [event-driven-execution-engines](concepts/event-driven-execution-engines.md).
*   **FVG RANSAC Logic Validation (March 25):** The implementation of absolute slope degree (`|β1|`) validation for tick-level regression has superseded discretionary FVG gap measurements. Void thresholding (≤ 0.00015 units/sec) has now become the fundamental gatekeeper for algorithmically recognizing true displacement [fvg-tick-level-ransac-degree](concepts/fvg-tick-level-ransac-degree.md).
*   **Numba Microstructure Adoption (March 24):** The open-source formalization of `FinMLKit` establishes a deterministic Python-native pipeline that finally replaces pandas-induced latency, permitting sub-microsecond event queueing [numba-hft-microstructure-bars](concepts/numba-hft-microstructure-bars.md).
*   **Execution Infrastructure Bounds (March 2026):** Reviews of current retail/pro-sumer execution networks (e.g., FXNX environments) confirm that strictly enforced 0.0 spread execution tiers are absolute mathematical requirements for high-frequency liquidity sweep logic, as spread-to-ATR ratio decay directly sabotages the baseline expectancy of 1:3 R:R scalps [retail-execution-infrastructure](recent/2026-03/retail-execution-infrastructure.md). 

---

## Open Questions / Counter-Evidence

*   **LLM Sentiment vs. Execution Latency:** The integration of LLM-based parsing (e.g., OpenAI models translating news strings into quantitative -1.0 to 1.0 matrices [sentiment-quantification-filter](concepts/sentiment-quantification-filter.md)) introduces significant API polling latency. Does the inclusion of unstructured text interpretation mathematically justify the loss of microsecond-level execution timing? Current evidence suggests it should only serve as a high-timeframe (HTF) context filter, strictly barred from the low-latency FSM trigger path.
*   **Black-Box "AI Signals" as Violation of Core Mandates:** Platforms deploying proprietary neural net outputs or "AI Signal streams" (observed in recent broker updates [execution-environments-broker-profiles](recent/2026-03/execution-environments-broker-profiles.md)) fundamentally violate the requirement for system determinism. If a signal tree cannot be mathematically unrolled and backtested out-of-sample via Walk-Forward Analysis, its inclusion structurally corrupts the risk encoding model.
*   **Options Dark Pool Latency Proxies:** While UOA (Unusual Options Activity) and Dark Pool print aggregation provide powerful institutional displacement proxies [institutional-orderflow-proxies](concepts/institutional-orderflow-proxies.md), the temporal reporting lag inherent to OTC prints threatens deterministic time-series alignment. Can these flows be modeled dynamically via Zero-Phase Filters (ZPF) without incurring forward-looking data contamination (look-ahead bias) during backtesting?
*   **Symbolic Regression Curve-Fitting Risks:** Although Genetic Programming (GP) yields novel formulaic indicators via symbolic regression [genetic-programming-structural-logic](concepts/genetic-programming-structural-logic.md), continuous out-of-sample survival validation is required. The risk of the algorithm organically "curve-fitting" to idiosyncratic data noise during the evolutionary loop remains a primary statistical failure mode that demands strict Sharpe/Drawdown penalizations in the fitness function.